const num1 = 5;
const num2 = 10;

function addValues(){
    console.log(`the sum is: ${num1 + num2}`)
}

addValues()

//este módulo define una función, que con solo requires se ejecutará por ser una función